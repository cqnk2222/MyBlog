# YangHao Notes

This directory is the source workspace for my GitBook notes.

Suggested structure:

- `preface.md`: overview and usage notes
- topic folders such as `machine-learning/` and `nlp/`
- each topic can include an index page and multiple note pages

The Astro site can keep `/notes` as a navigation entry, while GitBook uses the files here as the
documentation source.
